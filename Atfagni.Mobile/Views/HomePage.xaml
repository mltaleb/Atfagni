<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Atfagni.Mobile.ViewModels"
             xmlns:dto="clr-namespace:Atfagni.Shared.DTOs;assembly=Atfagni.Shared"
             x:DataType="vm:HomeViewModel"
             x:Class="Atfagni.Mobile.Views.HomePage"
             xmlns:converters="clr-namespace:Atfagni.Mobile.Converters"
             Title="Atfagni"
             BackgroundColor="#F8F9FA">
    <!-- Fond gris très léger Pro -->
   
    <ContentPage.Resources>
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <!-- Un petit convertisseur pour inverser les booléens si besoin -->
        <Style TargetType="Border">
            <Setter Property="StrokeShape" Value="RoundRectangle 15"/>
            <Setter Property="StrokeThickness" Value="0"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BackgroundColor" Value="White"/>
            <Setter Property="Shadow">
                <Shadow Brush="Black" Opacity="0.05" Radius="10" Offset="0,5"/>
            </Setter>
        </Style>
    </ContentPage.Resources>
    <Grid>
        <!-- ============================================= -->
        <!-- VUE CHAUFFEUR (Dashboard) -->
        <!-- ============================================= -->
        <!-- DANS LA VUE CHAUFFEUR -->
        <ScrollView IsVisible="{Binding IsDriver}">
            <VerticalStackLayout Padding="20" Spacing="25">

                <!-- 1. EN-TÊTE SIMPLIFIÉ (Solde uniquement) -->
                <Grid ColumnDefinitions="*, Auto">
                    <VerticalStackLayout>
                        <Label Text="Mon Solde Actuel" TextColor="Gray" FontSize="12"/>
                        <Label Text="{Binding DashboardData.WalletBalance, StringFormat='{0} DA'}" 
                       TextColor="{Binding DashboardData.WalletBalance}" 
                       FontSize="30" FontAttributes="Bold"/>
                    </VerticalStackLayout>

                    <!-- Avatar -->
                    <Border Grid.Column="1" StrokeShape="RoundRectangle 25" HeightRequest="50" WidthRequest="50" Padding="0">
                        <Image Source="user_circle.png" Aspect="AspectFill"/>
                    </Border>
                </Grid>

                <!-- 2. PROCHAIN DÉPART (Le Contexte) -->
                <Border BackgroundColor="#2196F3" StrokeThickness="0" Padding="20">
                    <Grid RowDefinitions="Auto, Auto" RowSpacing="10">
                        <Label Text="🚀 PROCHAIN DÉPART" TextColor="White" FontSize="10" FontAttributes="Bold" Opacity="0.8"/>

                        <Grid Grid.Row="1" ColumnDefinitions="Auto, *, Auto">
                            <Label Text="{Binding DashboardData.NextRide.StartLocation}" TextColor="White" FontSize="20" FontAttributes="Bold"/>
                            <Label Grid.Column="1" Text="➝" TextColor="White" HorizontalOptions="Center" FontSize="20" Opacity="0.6"/>
                            <Label Grid.Column="2" Text="{Binding DashboardData.NextRide.EndLocation}" TextColor="White" FontSize="20" FontAttributes="Bold"/>
                        </Grid>

                        <Label Grid.Row="2" Text="{Binding DashboardData.NextRide.DepartureTime}" TextColor="White" HorizontalOptions="Center" FontAttributes="Bold"/>
                    </Grid>
                </Border>

                <!-- 3. LISTE D'EMBARQUEMENT (Le Cœur du système) -->
                <Label Text="Passagers à valider" FontAttributes="Bold" TextColor="Gray" Margin="0,10,0,0"/>

                <CollectionView ItemsSource="{Binding PassengersToBoard}">
                    <CollectionView.EmptyView>
                        <VerticalStackLayout Spacing="10" Padding="30">
                            <Label Text="✅" FontSize="40" HorizontalOptions="Center"/>
                            <Label Text="Tout le monde est à bord !" HorizontalOptions="Center" TextColor="Gray"/>
                        </VerticalStackLayout>
                    </CollectionView.EmptyView>

                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="dto:BookingRequestDto">
                            <Border Margin="0,5" Padding="15" BackgroundColor="White" StrokeThickness="0">
                                <Grid ColumnDefinitions="50, *, Auto">

                                    <!-- Type (Passager ou Colis) -->
                                    <Border Grid.Column="0" BackgroundColor="#E3F2FD" HeightRequest="40" WidthRequest="40" StrokeShape="RoundRectangle 20" HorizontalOptions="Start">
                                        <Label Text="{Binding Type, Converter={StaticResource TypeToIconConverter}}" 
                                       HorizontalOptions="Center" VerticalOptions="Center" FontSize="18"/>
                                    </Border>

                                    <!-- Infos -->
                                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Margin="10,0">
                                        <Label Text="{Binding PassengerName}" FontAttributes="Bold" FontSize="16"/>
                                        <Label Text="{Binding PassengerPhone}" TextColor="Gray" FontSize="12"/>
                                        <!-- Description colis si existe -->
                                        <Label Text="{Binding PackageDescription}" TextColor="#2196F3" FontSize="11" IsVisible="{Binding PackageDescription, Converter={StaticResource StringNotEmptyConverter}}"/>
                                    </VerticalStackLayout>

                                    <!-- BOUTON VALIDER -->
                                    <Button Grid.Column="2" Text="VALIDER" 
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:HomeViewModel}}, Path=ValidatePassengerCommand}"
                                    CommandParameter="{Binding .}"
                                    FontSize="12" FontAttributes="Bold"
                                    BackgroundColor="#4CAF50" TextColor="White"
                                    HeightRequest="40" CornerRadius="20"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <!-- Bouton Publier (plus petit, en bas) -->
                <Button Text="+ Nouveau Trajet" Command="{Binding GoToPublishCommand}" 
                BackgroundColor="Transparent" TextColor="#2196F3" Margin="0,20"/>

            </VerticalStackLayout>
        </ScrollView>

        <!-- ============================================= -->
        <!-- VUE PASSAGER (Recherche) -->
        <!-- ============================================= -->
        <Grid RowDefinitions="Auto, *" IsVisible="{Binding IsPassenger}" Padding="20" RowSpacing="20">

            <!-- 1. En-tête Passager -->
            <VerticalStackLayout Grid.Row="0" Spacing="15">
                <Label Text="{Binding UserName, StringFormat='Bonjour {0},'}" FontSize="18" TextColor="Gray"/>
                <Label Text="Où voulez-vous aller ?" FontSize="28" FontAttributes="Bold" TextColor="Black"/>

                <!-- Boîte de Recherche (Style Carte) -->
                <Border  Stroke="Transparent" StrokeThickness="0" Padding="15" BackgroundColor="White" HorizontalOptions="Fill" VerticalOptions="Fill">
                    <VerticalStackLayout Spacing="10">
                        <!-- Départ -->
                        <Grid ColumnDefinitions="30, *">
                            <Label Text="📍" VerticalOptions="Center"/>
                            <Entry Grid.Column="1" Placeholder="Départ (ex: Tindouf)" Text="{Binding SearchFrom}" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#EEEEEE"/>

                        <!-- Arrivée -->
                        <Grid ColumnDefinitions="30, *">
                            <Label Text="🏁" VerticalOptions="Center"/>
                            <Entry Grid.Column="1" Placeholder="Arrivée (ex: Zouerate)" Text="{Binding SearchTo}" />
                        </Grid>
                        <BoxView HeightRequest="1" Color="#EEEEEE"/>

                        <!-- Date -->
                        <Grid ColumnDefinitions="30, *">
                            <Label Text="📅" VerticalOptions="Center"/>
                            <DatePicker Grid.Column="1" Date="{Binding SearchDate}" Format="dddd d MMMM" />
                        </Grid>

                        <!-- Bouton Rechercher -->
                        <Button Text="RECHERCHER" Command="{Binding SearchRidesCommand}" 
                                BackgroundColor="#2196F3" CornerRadius="10" Margin="0,10,0,0"/>
                    </VerticalStackLayout>
                </Border>
            </VerticalStackLayout>

            <!-- 2. Résultats de recherche -->
            <CollectionView Grid.Row="1" ItemsSource="{Binding SearchResults}" SelectionMode="None">
                <CollectionView.Header>
                    <Label Text="Trajets disponibles" FontAttributes="Bold" Margin="0,0,0,10"/>
                </CollectionView.Header>

                <CollectionView.EmptyView>
                    <Label Text="Lancez une recherche pour voir les trajets." 
                           HorizontalOptions="Center" VerticalOptions="Center" TextColor="Gray"/>
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="dto:RideDto">
                        <!-- CARTE TRAJET (Style Blablacar) -->
                        <Border Margin="0,5" Padding="15"  Stroke="Transparent" StrokeThickness="0" BackgroundColor="White" HorizontalOptions="Fill" VerticalOptions="Fill">
                            <Border.GestureRecognizers>
                                <!-- Clic sur la carte -> Détails -->
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:HomeViewModel}}, Path=GoToRideDetailCommand}"
                                                      CommandParameter="{Binding .}"/>
                            </Border.GestureRecognizers>

                            <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, Auto">
                                <!-- Heure et Trajet -->
                                <VerticalStackLayout Grid.Row="0">
                                    <Label Text="{Binding DepartureTime, StringFormat='{0:HH:mm}'}" FontAttributes="Bold" FontSize="18"/>
                                    <Label Text="{Binding StartLocation, StringFormat='{0} ➝ {1}'}" TextColor="Gray"/>
                                </VerticalStackLayout>

                                <!-- Prix -->
                                <Label Grid.Column="1" Text="{Binding Price, StringFormat='{0} DA'}" 
                                       FontAttributes="Bold" FontSize="18" TextColor="#2196F3"/>

                                <!-- Chauffeur -->
                                <HorizontalStackLayout Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10,0,0" Spacing="10">
                                    <Image Source="user_circle.png" HeightRequest="20"/>
                                    <Label Text="{Binding DriverName}" VerticalOptions="Center"/>

                                    <!-- Icône Colis si accepté -->
                                    <Border IsVisible="{Binding AcceptsPackages}" Padding="5,2" BackgroundColor="#E3F2FD"  Stroke="Transparent" StrokeThickness="0" HorizontalOptions="Start" VerticalOptions="Center">
                                        <Label Text="📦 Colis OK" FontSize="10" TextColor="#1976D2"/>
                                    </Border>
                                </HorizontalStackLayout>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </Grid>
</ContentPage>